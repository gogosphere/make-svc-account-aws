export GOPATH=/Users/whancock/Code/make-svc-account-aws
export GOBIN=/Users/whancock/Code/make-svc-account-aws/bin

if [ ! -d src ]; then
       	mkdir src
fi

if [ ! -d pkg ]; then
        mkdir pkg
fi

if [ ! -d bin ]; then
        mkdir bin
fi
